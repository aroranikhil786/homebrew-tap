# homebrew-tap

Homebrew formulae for [macstash](https://github.com/aroranikhil786/macstash).

```
brew install aroranikhil786/tap/macstash
```

Formulae here point at release tarballs published on the macstash repository
and pin their SHA-256 checksums, so `brew` verifies what it downloads.
